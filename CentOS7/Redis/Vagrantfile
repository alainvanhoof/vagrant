Vagrant.configure(2) do |config|

  config.vm.provider 'virtualbox' do |vm|
    vm.memory = 1024
  end

  config.vm.box = "centos/7"
  config.vm.hostname = "redis01"
  config.vm.network "private_network", ip: "10.0.0.130", netmask: "255.255.255.0", virtualbox_intnet: "redis_network"
  config.vm.provision "shell", path: "Provision.sh"

end
